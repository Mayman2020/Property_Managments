package com.propertymanagement.modules.maintenance.request.entity;

public enum RequestStatus {
    PENDING, ASSIGNED, SCHEDULED, IN_PROGRESS,
    TENANT_ABSENT, COMPLETED, CANCELLED, NEEDS_REVISIT
}
